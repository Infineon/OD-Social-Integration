/** FileName @pages.controller
 * author @sudhakar
 * <summary>
 *  DAO for performing Database related operation in DB for page schema
 *  <npm>
 *      @log4js {Used for logging debug,information and errors in page module}
 *  </npm>
 * </summary>
 */
 const log4js = require('log4js');
 const logger = log4js.getLogger("PagesController");
//  const pages = require('./model/pages.model');
